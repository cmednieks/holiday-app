<%= form_for([@holiday, @occurrence]) do |f| %>
  <% if @occurrence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@occurrence.errors.count, "error") %> prohibited this occurrence from being saved:</h2>

      <ul>
      <% @occurrence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :holiday_id, :value => @holiday.id %><br>
  </div>
  <div class="field">
	<% @day_options = CalendarDate.pluck(:day) %>
	<% @ids = CalendarDate.pluck(:id) %>
    <%= f.label "New occurrence date" %><br>
    <%= f.collection_select(:calendar_date_id, CalendarDate.all, :id, :day) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
